# CampusAR API

Express + TypeScript clean architecture service for CampusAR.

## Scripts

- `npm run dev` – watch mode
- `npm run db:migrate` – apply schema
- `npm run db:seed` – seed Smart Campus demo graph
- `npm test` – unit + integration tests
- Swagger: `http://localhost:4000/api/docs`
