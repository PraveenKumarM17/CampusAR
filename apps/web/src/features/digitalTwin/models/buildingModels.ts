/**
 * Optional per-building GLB/GLTF URLs under `/models/buildings/`.
 * Keep empty until real campus models exist — do not invent production assets.
 */
export const BUILDING_MODEL_URLS: Record<string, string> = {};
